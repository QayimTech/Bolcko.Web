@model int
@if (Model > 0)
{
    <span class="absolute -top-1.5 -end-1.5 min-w-5 h-5 px-1 bg-[#E8A020] text-[#151B26] text-[10px] font-black rounded-full border-2 border-[#151B26] flex items-center justify-center shadow-sm">@Model</span>
}
