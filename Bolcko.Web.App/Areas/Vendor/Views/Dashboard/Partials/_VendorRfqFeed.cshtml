@model Bolcko.Domain.Entities.Catalog.VendorProfile
@{
    var isArabic = System.Globalization.CultureInfo.CurrentCulture.Name.StartsWith("ar");
}

<div class="bg-white dark:bg-slate-900 rounded-3xl border border-slate-100 dark:border-slate-800 shadow-sm overflow-hidden">
    <div class="p-6 border-b border-slate-100 dark:border-slate-800 flex items-center justify-between">
        <div>
            <h2 class="text-base font-bold text-slate-900 dark:text-white">@(isArabic ? "أحدث طلبات عروض الأسعار والتوريد (Live Calculator RFQs)" : "Incoming Calculator RFQs in Your City")</h2>
            <p class="text-xs text-slate-500 dark:text-slate-400 mt-0.5">@(isArabic ? "طلبات حقيقية صادرة من عملاء أنهوا حساب كمياتهم في حاسبة البناء وينتظرون عروض التوريد" : "Real inquiries from clients who calculated quantities via our smart calculator")</p>
        </div>
        <a href="/calculator" target="_blank" class="text-xs font-bold text-amber-600 dark:text-amber-400 hover:text-amber-700 flex items-center gap-1">
            <span>@(isArabic ? "تجربة الحاسبة" : "Try Calculator")</span>
            <span class="material-symbols-outlined text-sm">open_in_new</span>
        </a>
    </div>

    <div class="divide-y divide-slate-100 dark:divide-slate-800">
        <div class="p-6 flex flex-col sm:flex-row justify-between sm:items-center gap-4 hover:bg-slate-50/60 dark:hover:bg-slate-800/30 transition-colors">
            <div class="space-y-1">
                <div class="flex items-center gap-2">
                    <span class="px-2.5 py-0.5 rounded-full text-[10px] font-bold bg-indigo-50 dark:bg-indigo-950/50 text-indigo-700 dark:text-indigo-300 border border-indigo-200 dark:border-indigo-800">@(isArabic ? "فيلا سكنية • 2 طوابق" : "Residential Villa • 2 Floors")</span>
                    <span class="text-xs font-bold text-slate-900 dark:text-white">@(isArabic ? "مشروع فيلا مسطح 500 م²" : "Villa 500 sqm Project")</span>
                    <span class="text-[11px] text-slate-400">• @(Model?.City ?? (isArabic ? "عمان" : "Amman"))</span>
                </div>
                <p class="text-xs text-slate-600 dark:text-slate-300">
                    <strong>@(isArabic ? "المواد المطلوبة:" : "Required Materials:")</strong> @(isArabic ? "حجر رويشد طبيعي مفجر (580 م²) • كرانيش 64 م طولي • حديد تسليح Grade 60 (20.5 طن) • باطون جاهز B250 (190 م³)" : "Natural Ruwaished Stone (580 m²) • Cornices 64 lm • Rebar Grade 60 (20.5 Ton) • Ready-mix B250 (190 m³)")
                </p>
                <span class="text-[10px] text-slate-400 font-mono">@(isArabic ? "منذ 25 دقيقة • عبر حاسبة بلوكو الذكية" : "25 mins ago • via BLOCKO Smart Calculator")</span>
            </div>
            <div class="flex items-center gap-2 flex-shrink-0">
                @if (Model?.Status == "Active")
                {
                    <button type="button" onclick="openBiddingModal('فيلا مسطح 500 م²', 'حجر رويشد مفجر 580م²، كرانيش، حديد، باطون', 'عمان')"
                            class="px-5 py-2.5 bg-slate-900 hover:bg-[#E8A020] hover:text-slate-950 text-white font-bold text-xs rounded-xl transition-all shadow-sm cursor-pointer flex items-center gap-1.5">
                        <span class="material-symbols-outlined text-sm">request_quote</span>
                        <span>@(isArabic ? "تقديم عرض سعر رسمي" : "Submit Official Quotation")</span>
                    </button>
                }
                else
                {
                    <button type="button" disabled
                            title="@(isArabic ? "الحساب بانتظار الاعتماد لتقديم العروض" : "Account pending verification")"
                            class="px-5 py-2.5 bg-slate-200 dark:bg-slate-800 text-slate-500 font-bold text-xs rounded-xl cursor-not-allowed flex items-center gap-1.5 border border-slate-300 dark:border-slate-700">
                        <span class="material-symbols-outlined text-sm">lock_clock</span>
                        <span>@(isArabic ? "بانتظار اعتماد الحساب للتقديم" : "Pending Verification")</span>
                    </button>
                }
            </div>
        </div>
    </div>
</div>
